<div class="card">
<div class="card-body">
        <h3>{{title}} <a href="/" class="exit-btn">X</a></h3>
        <p>{{content}}</p>
        <p>Created by <b>{{user.username}}</b> on {{format_dateAndTime createdAt}}</p>
</div>
</div>

{{> newComment}}

{{#each comments as |comment|}}
<div class="card">
<div class="card-body">
    <h4><b>{{user.username}}</b> {{format_dateAndTime createdAt}}</h4>
    <p>{{content}}</p>
</div>
</div>
{{/each}}