{{#each posts as |post| }}
<div class="card">
<div class="card-body">
  <a href="/post/{{post.id}}">
    <div>
      <p style="float:right;">Posted by <b>{{user.username}}</b> on {{format_date createdAt}}</p>
        <h3>{{post.title}}</h3>
    </div>
  </a>
</div>
</div>
{{/each}}